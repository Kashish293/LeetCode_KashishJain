class Solution {
public:
    double angleClock(int hour, int minutes) {
        double m=minutes*6;
        double h=hour*30+minutes*0.5;
        double diff=abs(h-m);
        double angle=min(diff,360-diff);
        return angle;

    }
};
